apiVersion: v2
name: react-frontend
description: A React single-page application served as a static build, exposed through the shared Gateway (HTTPRoute)
type: application
version: 0.1.0
appVersion: "1.0.0"
