apiVersion: v1
kind: Service
metadata:
  name: {{ include "dotnet-backend.fullname" . }}
  labels:
    {{- include "dotnet-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "dotnet-backend.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
